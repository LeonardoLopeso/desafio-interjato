import styled from 'styled-components';

export const Container = styled.div`
  
  .arrow-back {
    display: flex;
    flex-direction: row;
    align-items: center;
    max-width: 80px;
    margin-bottom: 1rem;
    cursor: pointer;
    color: var(--text-section);
  }

`;